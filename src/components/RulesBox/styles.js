import { StyleSheet } from "react-native";

export default StyleSheet.create({
  rules: {
    justifyContent: "center",
  },

  ruleText: {
    color: "#FFF",
    fontSize: 18,
    lineHeight: 21,
  },
});
